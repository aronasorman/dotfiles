interface:
  display_name: "Markdown Reader"
  short_description: "Render or author a readable Markdown page"
  default_prompt: "Use $markdown-reader to turn this into a self-contained, readable HTML page - render the Markdown as written, or write the explanation as Markdown first and render that."

policy:
  allow_implicit_invocation: true
