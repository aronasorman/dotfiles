interface:
  display_name: "Human-Driven PR Workflow"
  short_description: "Pair on critical code and ship one reviewed PR"
  default_prompt: "Use $human-driven-pr-workflow to help me write the critical implementation, complete bounded supporting work, verify the assembled change, shape the final JJ history, and prepare a ready-for-review PR for CodeRabbit."

policy:
  allow_implicit_invocation: false
